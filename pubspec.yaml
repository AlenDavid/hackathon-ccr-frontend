name: frontend
description: Hackathon CCR

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  http: ^0.12.1
  flexible_polyline: ^1.0.0-alpha           

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - images/logo.png

fonts:
  - family: ShadowsIntoLight
    fonts:
      - asset: fonts/ShadowsIntoLight-Regular.ttf

  - family: SourceSansPro
    fonts:
      - asset: fonts/SourceSansPro-Regular.ttf